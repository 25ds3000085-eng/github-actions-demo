name: Demo Action

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: 25ds3000085@ds.study.iitm.ac.in
        run: echo "Hello, world!"

      - name: Finish
        run: echo "Done"
